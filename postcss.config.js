/**
 * Tailwind CSS v4 is wired through `@tailwindcss/vite` in vite.config.ts.
 * This file exists so tooling that expects PostCSS can resolve config;
 * add PostCSS plugins here if you need them later.
 */
export default {
  plugins: {},
}
