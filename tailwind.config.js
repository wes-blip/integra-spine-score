/** @type {import('tailwindcss').Config} */
/**
 * Integra Health — Workday Spine Score
 * Brand tokens are defined in `src/index.css` @theme (Tailwind v4).
 * Sampled from `branding items/`: ink ≈ #212721, gold ≈ #cfb023.
 * Typography: Outfit (display), Source Sans 3 (body) — loaded in index.html.
 */
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
}
